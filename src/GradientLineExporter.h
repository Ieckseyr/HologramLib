#pragma once

namespace debugshape_export {

/**
 * GradientLineExporter - 导出渐变线条 API 给 LSE
 *
 * 统一命名空间 "HologramLib"（gradient* 前缀域）
 */
class GradientLineExporter {
public:
    static void exportAll();
};

} // namespace debugshape_export
